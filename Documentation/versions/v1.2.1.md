# Version 1.2.1

Version 1.2.1 was released on 2018/12/17.

## Changes in This Release
* Ensure bucket and legacy-disk equivalent API parity.
* Allow bucket creation from the connection object.
* Mark `StopPool` as obsolete (use `Close` and `Delete` instead).
* Add `Close` and `Delete` pool behavior.
