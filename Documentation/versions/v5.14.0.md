# Version 5.14.0

Version 5.14.0 was released on 2025/05/22.

## Changes in This Release
* Add new `ComputingQuotas` field in user info, gathering quotas for different scheduling types (flex, onDemand, reserved).
* Add organization quotas in user's `ComputingQuotas` field, inherited from the user's organization if applicable.
