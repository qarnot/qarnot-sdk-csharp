# Version 6.3.0

Version 6.3.0 was released on 2026/05/04.

## Changes in This Release
* Mark `FileCount` and `UsedSpaceBytes` as obsolete on `AbstractStorage` and `QBucket`. These fields can be costly to retrieve from a remote S3 service; use client-side iteration over the bucket to compute these statistics if needed.
